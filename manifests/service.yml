apiVersion: v1
kind: Service
metadata:
    name: revaturecinephiliacscinephiliacsrepo
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: revaturecinephiliacscinephiliacsrepo